est ce que tu peut me lister toute les type d'element qu'on  peut manipuler actuelement?


lister seulemnt les actifs qui sont dans l’API GLPI disponible mais pas encore utilisés.

est ce qu'il y a d'autre dans l'API v1, je parle des actifs uniquement

fais sortir la liste “actifs” en version strict (uniquement les classes d’actifs principales GLPI) vs version large (incluant tous les objets réseau/infra ci-dessus).


est ce qu'on peut utiliser les API v1 et v2.3 en meme temps lors de l'import car je veux ajouter les elements suivant ce qui est afficher dans la liste deroulant des elements dans l'interface de glpi lors de la creation d'une ticket:

- Baies

- Base de donnee

- Chassis

- Logiciel

- Salle Serveur